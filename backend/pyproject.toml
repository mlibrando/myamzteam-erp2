[project]
name = "myamzteam-erp"
version = "0.1.0"
description = "MYAMZTEAM Amazon P&L ERP — sync + API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "psycopg[binary]>=3.2",
    "openpyxl>=3.1",
    "python-dotenv>=1.0",
    "alembic>=1.18",
    "sqlalchemy>=2.0",
    "python-amazon-sp-api>=2.1.8",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*", "sync*", "db*"]
