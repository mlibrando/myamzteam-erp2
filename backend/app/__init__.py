"""v1 P&L dashboard API — read-only FastAPI endpoint over pnl_monthly + ad_spend_daily.

Built against reference/data/pnl_dashboard_probe.md (the mapping is that doc's, verbatim).
Read-only: no writes to any table.
"""
